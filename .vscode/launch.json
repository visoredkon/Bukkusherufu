{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Start API",
            "type": "node",
            "request": "launch",
            "program": "${workspaceFolder}/src/index.js",
            "cwd": "${workspaceFolder}"
        }
    ]
}
